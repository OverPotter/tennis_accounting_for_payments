server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: "tennis_telegram_bot"
    static_configs:
      - targets:
          - localhost
        labels:
          job: "tennis_bot"
          __path__: /var/log/tennis_bot/*.log
